{
  "private": true,
  "license": "MIT",
  "workspaces": [
    "theme",
    "example"
  ],
  "scripts": {
    "lint": "yarn workspace gatsby-theme-dev lint",
    "test": "yarn workspace gatsby-theme-dev test",
    "develop": "yarn workspace example develop",
    "build": "yarn workspace example build"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.2.0",
    "@commitlint/config-conventional": "^8.2.0",
    "husky": "^3.0.9",
    "lerna": "^3.18.3"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
