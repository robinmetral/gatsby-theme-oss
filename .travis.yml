language: node_js
node_js:
  - 12.9.0

cache: yarn

notifications:
  email: false

install:
  - yarn install --frozen-lockfile

jobs:
  include:
    - stage: test
      script:
        - yarn lint
        - yarn test
        - yarn coverage

    - stage: build
      script:
        - gatsby build
      after_success:
        - yarn run audit --perf 90 --pwa 100 --a11y 90 --bp 90 --seo 90 --runner=wpt "$(wait-for-netlify-preview)"
